//import { app } from '../utilities/init-firebase'

//export const addQuiz = async (quizData) => {
  //let response = app.firestore().collection('quiz').add(quizData);
 // return response;
//};

///export const getAllQuiz = async () => {
 // const snapshot = app.firestore().collection('quiz').get();
 // const quiz = snapshot.docs.map((doc) => ({ id: doc.id, ...doc.data() }));
 // return quiz;
//};

//export const getAllUsers = async () => {
//  const snapshot = app.firestore().collection('users').get();
//  const users = snapshot.docs.map((doc) => ({ id: doc.id, ...doc.data() }));
//  return users;
//}